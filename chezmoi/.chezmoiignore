# Ignore READMEs and documentation. The pattern needs the ** prefix: a bare
# README.md only matches the one at the root, not e.g. .config/sway/README.md.
**/README.md
README.md
LICENSE

# Repo documentation that happens to sit next to the dotfiles. Without this it
# lands in $HOME as ~/i3-to-sway.md, which is exactly where notes go to die.
i3-to-sway.md

# Ignore NixOS files (applied separately via nixos-rebuild)
nixos/

# OS-specific ignores
{{ if ne .chezmoi.os "linux" }}
.Xresources
.config/sway/
.config/quickshell/
.config/waybar/
.config/mako/
.local/bin/signal-desktop
.local/share/applications/
{{ end }}

{{ if ne .chezmoi.os "darwin" }}
# macOS-specific files
{{ end }}
